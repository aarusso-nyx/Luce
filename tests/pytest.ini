[pytest]
addopts = -ra
markers =
  build: Build validation tests.
  hil: Hardware-in-loop tests.
  contract: API/protocol contract tests.
  net: Network transport tests.
  slow: Longer-running tests.
