# Stage5 HIL evidence: cli_wifi_status

Timestamp: __REPLACE_WITH_TS__
Environment: luce_stage5
Port: /dev/cu.usbserial-40110

Checkpoint commands to capture:
1) `wifi.status` after boot
2) `wifi.status` after disconnect event
3) `wifi.status` while reconnecting
4) `wifi.status` after reconnection

Expected CLI response shape:
- line containing `CLI cmd='wifi.status'`
- `[WIFI][STATUS] state=<...> enabled=<0|1> attempt=<N> ...`
- stable IP fields when connected

Observed output:
<paste timed command/output transcript here>
